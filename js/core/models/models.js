var Lecture = Backbone.Model.extend({
    initialize: function() {
    },
    defaults: {
        complete:false
    }
});

var Section = Backbone.Model.extend({
    initialize: function() {
    },
    defaults: {
        complete:false
    }
});

var Content = Backbone.Model.extend({
    initialize: function() {
    },
    defaults: {
        complete:false
    }
});